<div class="w-100 d-flex flex-column align-items-center my-5">

  <h1 class="mb-5 text-center">Edit Profile</h1>

  <%= render 'form' %>

  <div class="mt-5">
    <%= button_to "Delete Profile", user_path(current_user), class: "btn btn-danger", method: :delete, data: { confirm: "Are you sure?" } %>
  </div>
</div>